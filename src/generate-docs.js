function generateContributionFileFromConfig(name, sub, options) {
  const configFile = options.config;
  console.log('configFile', configFile);
  process.exit(0);
}

module.exports = generateContributionFileFromConfig;
